{
  "name": "extension-directory-sync",
  "version": "0.0.0",
  "private": true,
  "description": "Workspace-spanning directory sync for Frontier.",
  "scripts": {
    "//": "Local verification only — the host builds the real bundles itself (see README). `verify` reproduces the production directory nesting in a temp .verify/ mirror, runs tsc (host-side server+worker + ui) against it so `../../types` resolves exactly as the host resolves it, then esbuilds every entry the way the host's bundler does. `build:check` runs just the esbuild pass.",
    "verify": "node scripts/verify.mjs",
    "build:check": "node scripts/build-check.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
